<h2>Encontros</h2>

<% if @meetings.present? %>

  <% @meetings.each do |meeting| %>
    <h3><%= link_to meeting.title, meeting %></h3>
    <p>dia <%= meeting.date %> por <%= meeting.autor %></p>
    <%= meeting.description %>
    <br/>
    <%= print_present(meeting.code,"Código") %>
    <br/>
    <%= print_present(meeting.video,"Vídeo") %>
    <br/>
    <%= print_present(meeting.slide,"Slide") %>

  <% end %>
<% end %>
