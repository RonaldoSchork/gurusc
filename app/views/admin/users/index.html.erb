<h2>Usuários</h2>

<ul class="list">
  <% @users.each do |user| %>
    <%= content_tag_for :li, user, :class => cycle(:odd, :even) do %>
      <%= "#{user.full_name} (#{user.email})" %>

      <div class="right">
        <%= link_to_edit [:edit, :admin, user] %>
        <%= link_to_destroy [:admin, user] unless current_user == user %>
      </div>
    <% end %>
  <% end %>
</ul>

<hr />

<div class="bar">
  <div class="right">
    <%= link_to 'Novo usuário', new_admin_user_path, :class => "button" %>
  </div>
</div>
